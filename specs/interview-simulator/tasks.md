# Interview Simulator Tasks

- [ ] Add pre-commit hook support for linting and formatting.
- [ ] Add type checking via `tsc --noEmit`.
- [ ] Add secret scanning via `gitleaks detect --source .`.
- [ ] Create a feature spec directory under `specs/`.
- [ ] Verify repository metadata and tag support.
